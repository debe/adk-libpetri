/**
 * Bridges between ADK events and libpetri observability surfaces.
 *
 * <p>The observability APIs in this package are part of the settled 0.x
 * surface. The project is 0.x, so a minor may still break API; any such change
 * is called out in the CHANGELOG.
 */
package org.libpetri.adk.bridge;
